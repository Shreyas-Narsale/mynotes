
/* Name of file is same as compoenet name*/
const Header =()=>{/*Alaways have one parent div tag that inside that everything is avaiable */
    return(
        <div className="app-header">
            <h1>Notes List</h1>
        </div>
    )
}

export default Header;